#!/bin/bash

export OPENSSL_CONF=/etc/ssl
markdownpdf=$HOME/.local/lib/node_modules/markdown-pdf/bin/markdown-pdf
input=$1
output=$2
csspath="$HOME/.local/lib/mkd2pdf/css/github.css"

node "$markdownpdf" "$input" -o "$output" -s "$csspath"
